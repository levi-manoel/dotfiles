#!/usr/bin/env sh

# Source of truth for UI theme across apps.
# Values: "dark" | "light"
THEME="dark"

if [ "$THEME" = "dark" ]; then
  export GTK_THEME="Adwaita:dark"
  export QT_STYLE_OVERRIDE="Adwaita-Dark"
else
  export GTK_THEME="Adwaita"
  export QT_STYLE_OVERRIDE="Adwaita"
fi

