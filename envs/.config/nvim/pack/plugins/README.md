# Native Neovim packages

Plugins are loaded from `pack/plugins/start/` (Neovim’s built-in package system).

**First-time setup:** install plugins with:

```bash
./pack/plugins/install.sh
```

Then run Neovim. Optional: `:TSUpdate` for treesitter parsers.
