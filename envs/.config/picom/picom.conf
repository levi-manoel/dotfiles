# Minimal compositor for Slack overlays / screen draw.
# use-damage=false avoids SWT/Java flicker (e.g. DBeaver) on NVIDIA+GLX.

backend = "glx"
vsync = true;
use-damage = false;
shadow = false;
fading = false;
unredir-if-possible = false;
