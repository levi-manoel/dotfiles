#! /usr/bin/env bash

if [[ -z "$1" ]]; then
  echo "provide an argument"
  exit 1
fi

if [[ "$1" != 0 && "$1" != 1 ]]; then
  echo "arg should be 0 | 1"
fi

xinput set-prop 'VEN_04F3:00 04F3:3259 Touchpad' 'Device Enabled' $1
